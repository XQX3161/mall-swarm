spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mallswarm?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.macro.common.entity

server:
  port: 8080
