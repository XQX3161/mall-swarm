<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.common.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.macro.common.entity.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="email" column="email" />
        <result property="phone" column="phone" />
        <result property="status" column="status" />
        <result property="lastLogin" column="last_login" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, email, phone, status, last_login, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.macro.common.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, email, phone, status, last_login, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{status}, #{lastLogin}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateById" parameterType="com.macro.common.entity.User">
        UPDATE `user` SET
        username = #{username},
        password = #{password},
        email = #{email},
        phone = #{phone},
        status = #{status},
        last_login = #{lastLogin},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM `user` WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="long" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM `user` WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM `user` WHERE username = #{username} LIMIT 1
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM `user` ORDER BY id DESC
    </select>

</mapper>
